<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    >
    <!-- fill color -->
    <solid
        android:color="#ffffff" />
    <!-- radius -->
    <stroke
        android:width="6dp"
        android:color="#000000"/>
    <!-- size -->
    <size
        android:height="140dp"
        android:width="135dp"
        />
    <gradient
        android:angle="90"
        android:startColor="@color/colorRed_start"
        android:centerColor="@color/colorRed_final"/>
</shape>